"use strict";  // treat all js code as newer version/js

// alert("hello") // we are using nodejs, not browser

console.log("Answer is",3+3)

// code readability should be high in coding! must!

console.log("hello")

// primitive datatypes -

let name = "Professor"  // string datatype which can be written in '' and "" both prefered ( "" )

let age = 19    // number datatype for bigger number we should use 'bigint' datatype

let isLoggedIn = false  // this is boolean which shows yes/no simply true/false
 
//null    // basically it's a type and it's a standalone value in javascript and it's a representation of an empty value in programming world! and it can be assigned for e.g - let state = null and main thing " NULL is OBJECT "

//undefined // means the value is not defined and can called as - let accountState; means the value of accountState is not defined so it's called as undefined in programming

//symbol    // used for uniqueness..


// object

console.log(typeof(age));    // shows the type by writing the name of variable and other ways to show are - 
console.log(typeof "aayush");
console.log(typeof 19);
console.log(typeof true);   // etc...
console.log(typeof(null));  // object   // it shows object in o/p because Null is object
console.log(typeof(undefined)); // undefined

